name: narada

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: narada-app:local
    container_name: narada-app
    restart: unless-stopped
    read_only: true
    tmpfs:
      - /tmp:rw,nosuid,size=${APP_TMPFS_SIZE:-64m}
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    pids_limit: ${APP_PIDS_LIMIT:-256}
    mem_limit: ${APP_MEM_LIMIT:-768m}
    mem_reservation: ${APP_MEM_RESERVATION:-512m}
    cpus: "${APP_CPUS_LIMIT:-1.0}"
    environment:
      SPRING_PROFILES_ACTIVE: ${SPRING_PROFILES_ACTIVE:-docker}
      JAVA_OPTS: "${JAVA_OPTS:--XX:+UseG1GC -XX:InitialRAMPercentage=10.0 -XX:MaxRAMPercentage=60.0 -XX:MaxGCPauseMillis=200 -XX:+ExitOnOutOfMemoryError}"
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB:-narada}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-narada}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-narada_dev_password}
    ports:
      - "${APP_PORT:-7621}:8080"
    depends_on:
      postgres:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/actuator/health | grep -q '\"status\":\"UP\"'"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 40s
    networks:
      - narada-net

  postgres:
    image: postgres:17-alpine
    container_name: narada-postgres
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    pids_limit: ${POSTGRES_PIDS_LIMIT:-256}
    mem_limit: ${POSTGRES_MEM_LIMIT:-512m}
    mem_reservation: ${POSTGRES_MEM_RESERVATION:-256m}
    cpus: "${POSTGRES_CPUS_LIMIT:-1.0}"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-narada}
      POSTGRES_USER: ${POSTGRES_USER:-narada}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-narada_dev_password}
    ports:
      - "${POSTGRES_PORT:-7622}:5432"
    volumes:
      - narada-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-narada} -d ${POSTGRES_DB:-narada}"]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 15s
    networks:
      - narada-net

volumes:
  narada-postgres-data:

networks:
  narada-net:
    name: narada-net
